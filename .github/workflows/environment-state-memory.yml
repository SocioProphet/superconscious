name: environment-state-memory

on:
  pull_request:
    branches: [ main ]
    paths:
      - "examples/environment-state-memory-event.*.json"
      - "scripts/validate-environment-state-memory.py"
      - ".github/workflows/environment-state-memory.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate-environment-state-memory:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Validate environment state memory fixtures
        run: python3 scripts/validate-environment-state-memory.py
