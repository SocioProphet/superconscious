name: Trust Surface

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  validate-trust-surface:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Validate trust surface
        run: python3 scripts/validate-trust-surface.py .
