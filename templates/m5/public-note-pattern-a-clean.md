# Feature steering on refuse-and-redirect: an in-envelope intervention

## Abstract

This note reports a certified implementation of the implementability test for refuse-and-redirect feature steering. The central question is not only whether steering changes behavior, but whether the steered activation profile remains inside the model's natural activation envelope.

## One conceptual move

Feature steering needs an implementability test.

## Load-bearing figure

Insert the five-curve implementability plot showing studied-feature steering remaining inside the declared natural-baseline threshold through `alpha_behavioral`.

## Result frame

Pattern A: the studied feature's steering effect is achieved in-envelope, with no off-target damage detected by M1D.

## Interpretation

This strengthens the feature-steering story by adding structural evidence absent from ordinary steering reports. The result says the intervention selects a behavior the model can naturally produce, at the declared metric resolution.

## Limitations

No cross-model generalization is claimed. Cross-width robustness remains limited to the declared source-locked evidence. Transcoder/crosscoder evidence is deferred until public artifacts exist.
